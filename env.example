# Shopify Source Store Configuration
# Replace with your source Shopify store domain
SOURCE_SHOP=your-source-shop.myshopify.com
# Replace with your source store Admin API access token
SOURCE_TOKEN=your-source-access-token

# Shopify Target Store Configuration  
# Replace with your target Shopify store domain
TARGET_SHOP=your-target-shop.myshopify.com
# Replace with your target store Admin API access token
TARGET_TOKEN=your-target-access-token

# Optional: API Version (defaults to 2024-10)
# You can change this to a different API version if needed
API_VERSION=2024-10
